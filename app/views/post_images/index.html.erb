<!--PostImageの全レコードから1レコードずつ排出-->
<% @post_images.each do |post_image| %>
<div>
  <%= link_to post_image_path(post_image.id) do %>
    <%= image_tag post_image.get_image %>
  <% end %>

  <!--現在each中の投稿 に紐づくuser(アソシエーション)を呼び出し、さらにget_profile_imageを呼び出し-->
  <p>投稿ユーザー画像：<%= image_tag post_image.user.get_profile_image("100x100") %></p>
  <p>ショップ名：<%= post_image.shop_name %></p>
  <p>説明：<%= post_image.caption %></p>
  <!--user_idカラムでアソシエーションしてるUserモデルのnameカラムを呼び出し-->
  <p>ユーザーネーム：<%= post_image.user.name %></p>
  <!--each中の投稿に結びつくコメント(N)の数をカウント⇒変数展開-->
  <p><%= link_to "#{post_image.post_comments.count}コメント",post_image_path(post_image.id) %></p>
</div>
<% end %>
